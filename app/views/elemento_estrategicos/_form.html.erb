<%= form_for(@elemento_estrategico) do |f| %>
  <% if @elemento_estrategico.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@elemento_estrategico.errors.count, "error") %> prohibited this elemento_estrategico from being saved:</h2>

      <ul>
      <% @elemento_estrategico.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :texto %><br />
    <%= f.text_field :texto %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
