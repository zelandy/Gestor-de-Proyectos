<div class="well form-inline">
	<%= f.link_to_remove "x", :class=>'close' %>
	<br />
	<div class="control-group">
		<div class="controls input-prepend">
    		<%= link_to '#', :class=>'btn triggerFindContacto' do %>
				<i class='icon-book'></i>
			<% end %><%= text_field_tag :contacto_nombre, {}, :disabled=>'disabled', :class=>'disabled contacto_nombre' %>
			<%= f.input :contacto_id, :as=>'hidden' %>
		</div>
	</div>
	<%= f.input :role, :collection => CharterContacto::ROLES, :include_blank => false %>
</div>
<div class="modal hide fade" id="findContacto">
	 <div class="modal-header">
    <a class="close" data-dismiss="modal">×</a>
    <h3>Buscar contacto</h3>
  </div>
  <div class="modal-body">
	<div class="input-append">
		<%= text_field_tag :q %><%= link_to url_for(:controller=>'sphinx', :action=>:contacto), {:remote => true, :class=>'btn submit_link'}  do %>
			<i class='icon-search'></i>
		<% end %>
	</div>
	<div>
		<br />
		<table id="query_contacts" class="table table-condensed">
			
		</table>
	</div>
  </div>
  <div class="modal-footer">
    <a href="#" class="btn" rel="no-follow" data-dismiss="modal">Cerrar</a>
  </div>
</div>